---
'@relayprotocol/relay-kit-ui': patch
---

Remove the Porto wallet chain restrictions from the swap widget, since Ithaca has sunset Porto. The widget no longer treats `porto` or `portoevm` wallets as limited to a fixed set of chains.

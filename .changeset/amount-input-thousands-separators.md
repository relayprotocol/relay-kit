---
'@relayprotocol/relay-kit-ui': patch
---

Display thousands separators in the Swap Widget amount inputs (e.g. `183,428.03` instead of `183428.03`). Separators are display-only; the underlying amount values are unchanged.

---
"@relayprotocol/relay-ton-wallet-adapter": minor
"@relayprotocol/relay-sdk": minor
"@relayprotocol/relay-kit-ui": minor
---

Add TON support: new `@relayprotocol/relay-ton-wallet-adapter` package exporting `adaptTonWallet`, plus `tonvm` support across the SDK (vmType, TON receipt, transaction step messages, dead address) and UI kit (native TON balance, token selector).

---
'@relayprotocol/relay-sdk': patch
---

Surface the origin tx hash with a pending status for HyperCore origins as soon as the send lands on Hyperliquid, instead of waiting for the first Relay status that carries one.
